# Question 2: Create a function to greet a user by name

def greet_user(name):
    """
    Function to greet a user with their name.
    Args:
        name (str): The name of the user
    """
    print(f"Hello, {name}! Welcome to Python.")

# Call the function with a sample name
greet_user("Alice")
